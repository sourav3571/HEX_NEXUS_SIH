# --- START: CPU-only PyTorch (MUST BE AT THE TOP) ---
# Use --extra-index-url to ADD the PyTorch index, not replace the main one.
# This allows pip to find all other packages on the standard PyPI.
--extra-index-url https://download.pytorch.org/whl/cpu

# --- PyTorch (CPU-only) ---
# The '+cpu' suffix ensures the CPU-specific build is selected.
torch==2.8.0+cpu
torchvision==0.23.0+cpu

# --- Machine Learning & AI Frameworks ---
# This line requires 'git' to be installed in the build environment.
clip @ git+https://github.com/openai/CLIP.git@dcba3cb2e2827b402d2701e7e1c7d9fed8a20ef1
faiss-cpu
scikit-learn==1.5.0  # Note: 1.7.2 is not on PyPI, 1.5.0 is the latest stable.

# --- Web Framework (FastAPI) & Server ---
fastapi==0.111.0 # Note: Versions pinned to latest stable releases
gunicorn
uvicorn==0.30.1
python-multipart==0.0.9
starlette==0.37.2
pydantic==2.8.2
anyio==4.4.0

# --- Database & Backend Utilities ---
passlib
sqlalchemy
bcrypt
psycopg2-binary

# --- Data Science & Numerics ---
numpy==1.26.4 # Note: numpy 2.x has breaking changes, pinning to 1.x is safer for now
scipy==1.13.1
joblib==1.4.2
networkx==3.3
sympy
tqdm==4.66.4

# --- Core Utilities & Dependencies ---
opencv-python-headless==4.10.0.84
pillow==10.4.0
python-dotenv==1.0.1
google-generativeai
requests
email-validator==2.2.0
svgwrite